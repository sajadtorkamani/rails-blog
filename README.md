## Local setup

### Install gems
```bash
./bin/bundle install
```

### Create database
Make sure you've configured DB credentials in `config/database.yml` and then run:

```bash
./bin/rails db:create
```

## Deployment

TODO